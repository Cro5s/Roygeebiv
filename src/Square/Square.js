const COLORS = ["Red", "Orange", "Yellow", "Green", "Blue", "Indigo", "Violet"];
class Square {
  constructor(color) {
    this.color = color;
    // this.val = null;
    // switch (this.color) {
    //   case "Red" :
    // }
  }
}

export default Square;
